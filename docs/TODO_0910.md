# 3일차 (2026-09-10) 할 일 — 회의 결정 · 남은 백로그 · 발견한 문제

작성: 2026-09-10 · 작성자: 담당자 1 (배태일)
기준: 브랜치 `VsPeristalsis_dashboard` = `528963c` (푸시 전 커밋 8개)
관련 문서: [BACKLOG.md](BACKLOG.md) (9/9 피드백과 미결 사항) · [PRD_090910.md](PRD_090910.md)

> 오늘 회의에서 **메뉴 구조와 역할 분담이 새로 정해졌다.** 이 문서가 오늘의 기준이다.
> 백로그의 항목 중 오늘 살아 있는 것만 §4 로 옮겨 적었다.

---

## 0. 역할 분담과 한눈에

| 사람 | 맡은 것 |
|---|---|
| **배태일 (담당자 1)** | VS Code 연동 마무리, **메뉴 바 정리** |
| **조웅식 (담당자 2)** | **관리자 콘솔 구현** |

| 구분 | 항목 | 상태 |
|---|---|---|
| **회의 결정** | 메뉴 4개로 재구성 (§1-2) | 🔴 착수 (담당자 1) |
| | 사원번호 로그인 + 설정의 GitHub 연동 (§1-1) | 🟡 **방식 확정.** V4 마이그레이션 대기 |
| | 설정에서 GitHub·VS 연동 (§1-1) | 🔴 미착수 (담당자 1) |
| | 관리자 콘솔 (§1-3) | 🔴 미착수 (담당자 2) |
| **오늘 한 일** | VS 연동 8건 — 설치·사이드바·미푸시·근거 패널 | ✅ 완료 (§2) |
| **발견한 문제** | **수집기가 기본 브랜치만 본다** — 작업 브랜치 커밋이 일지에 없다 | 🔴 담당자 2 (§3-4) |
| | 18:00 자동 생성이 "버튼 눌러 생성" 과 충돌 | ❓ 기획 판단 (§3-1) |
| | 미푸시 커밋을 보낼 자리가 계약에 없음 | 🔴 계약 변경 (§3-2) |
| | 초안 템플릿이 계획 여러 건을 한 줄로 붙임 | 🟡 담당자 2 (§3-3) |
| **백로그 잔여** | F-1 에이전트 요약 · F-2 diff 표시 | 🔴 다음 사이클 (§4) |
| | §3-1 리포 삭제 소유자 검사 · §3-2 notify 확정 검사 | 🔴 미수정 (담당자 2) |
| | Mattermost 실전송 | ⏸ webhook 대기 |

---

## 1. 오늘 회의에서 정한 것

### 1-1. 로그인 · 회원 (담당자 1 / 회원 조회 API 는 담당자 2)

| 회의 내용 | 지금 상태 | 할 일 |
|---|---|---|
| 회원 조회 API 를 주면 **그 기준으로 로그인** | GitHub OAuth 로그인이 곧 회원가입이다 | 자체 로그인 화면 → 회원 조회 API 로 검증 → JWT |
| 설정에서 **GitHub · VS 연동** | GitHub 은 로그인 수단이고, VS 는 API Key 발급만 있다 | 설정에 "연동" 영역 신설. GitHub 은 로그인 → **내 계정에 연결**로 의미가 바뀐다 |
| 로그인 화면 **아래에 관리자 콘솔** 진입 | 로그인 화면은 "GitHub로 로그인" 버튼 하나 | 로그인 폼 아래에 관리자 콘솔 링크 |

#### 회원 조회 API — 받았다 (9/10 10:44)

| 항목 | 값 |
|---|---|
| BASE URL | 사내 API 서버. **주소는 `backend/.env` 의 `WAPLE_API_BASE_URL` 에만 둔다** |
| 인증 | 헤더 `X-Api-Key: <키>`. **키는 `backend/.env` 의 `WAPLE_API_KEY` 에 둔다 — 커밋 금지** |

| 기능 | Method | 경로 |
|---|---|---|
| 사용중인 와플 회사 목록 | GET | `/core/v1/companies` |
| 특정 회사의 재직중 사원 목록 | GET | `/core/v1/companies/{coSeq}/employees` |
| 특정 사원 이름 조회 (사용유무 무관) | GET | `/core/v1/companies/{coSeq}/employees/{empSeq}` |

**응답 모양** — 두 엔드포인트를 실제로 호출해 확인했다. 값은 사내 정보라 적지 않는다.

```
{ status: boolean, message: string, data: { <이름>: [...] } }

  /companies            → data.companies[]  { coSeq: number, coNm: string }
  /companies/{n}/employees → data.employees[] { empSeq: number, empNm: string }
```

### 로그인 방식 — 정해졌다 (9/10)

**GitHub 로그인을 걷어내고 사원 정보로 로그인한다.**

| 항목 | 정한 것 |
|---|---|
| 아이디 | 사원번호 (`empSeq`) — **네 자리 형식** |
| 최초 비밀번호 | 아이디와 같다 (사원번호 네 자리) |
| 이후 | **각자 설정 탭에서 비밀번호를 바꾼다** |
| GitHub | 로그인 수단이 아니라 **설정 탭의 연동**으로 내려간다 |
| 관리자 콘솔 | 최초 `admin` / `admin`. 비밀번호는 설정 탭에서 변경 |

**"네 자리 형식"의 뜻**: `empSeq` 는 1·3·22·100 처럼 자리수가 제각각이다. 앞을 0 으로 채워
`0001`·`0003`·`0022`·`0100` 으로 쓴다는 뜻으로 읽었다. 다르면 알려 주기 바란다.

#### 이 결정으로 필요해지는 것

| 영역 | 내용 | 담당 |
|---|---|---|
| 스키마 | `users` 에 `emp_seq`·`password_hash`·`role`·`must_change_password` 추가. **V4 마이그레이션** | 담당자 2 |
| 인증 | 아이디·비밀번호 로그인 → JWT. GitHub OAuth 콜백은 "로그인" → **"내 계정에 GitHub 연동"** 으로 의미가 바뀐다 | 담당자 2 |
| 화면 | 로그인 폼(아이디·비밀번호), 비밀번호 변경, 설정의 GitHub 연동 | 담당자 1 |
| 계정 생성 | 와플 사원 목록을 읽어 `users` 를 채운다. 언제 채울지 정해야 한다 (최초 로그인 시 자동 생성 / 관리자가 일괄 등록) | 담당자 2 |

#### 짚어 둘 것 — 최초 비밀번호가 공개 정보다

사원번호는 **사원 목록 API 로 누구나 조회할 수 있다.** 즉 최초 비밀번호는 비밀이 아니다.
아직 로그인하지 않은 사람의 계정에 **다른 사람이 먼저 들어갈 수 있다.** `admin`/`admin` 도 같다.

**변경을 강제하지 않기로 했다** (9/10 결정). 사내망 도구라 위험이 크지 않다는 판단이다.
대신 설정의 비밀번호 카드에서 "아직 최초 비밀번호를 쓰고 있다" 고 한 줄로 알린다.
`must_change_password` 는 통과를 막는 데 쓰지 않고 이 안내 조건으로만 쓴다.

> 아래 §1-1 의 "인증에 쓸 값이 없다" 분석은 이 결정 **이전**의 것이다. 기록으로 남긴다.

### (기록) 회원 조회 API 만으로는 인증이 안 된다

**사원 정보에 `empSeq` 와 `empNm` 밖에 없다.** 이메일도, 비밀번호도, 부서도, GitHub 계정도
없다. 이 API 만으로는 **본인 확인이 불가능하다** — 목록에서 자기 이름을 고르는 것은 인증이
아니라 선택이다. 아무나 남의 이름을 고를 수 있다.

게다가 **같은 이름을 가진 사원이 실제로 있다** (`empSeq` 가 다른 동명이인). 이름만으로는
사람을 특정할 수도 없다.

그래서 로그인은 이 API 하나로 끝나지 않는다. 적어도 아래 중 하나가 더 필요하다.

| 방법 | 내용 | 비용 |
|---|---|---|
| **A. 사원 선택 + GitHub OAuth** | 목록에서 자기 사원을 고르고, **GitHub 로그인으로 본인을 증명**한다. 한 번 이으면 다음부터 GitHub 로그인만으로 들어온다 | 지금 있는 GitHub OAuth 를 그대로 쓴다. **가장 싸다** |
| B. 초대 코드 | 관리자가 사원별 1회용 코드를 발급 | 관리자 콘솔에 발급 화면이 필요하다 |
| C. 와플 인증 API | 와플 쪽에 로그인 검증 API 가 따로 있는지 확인 | 있으면 가장 깔끔하다. **먼저 물어볼 값어치가 있다** |

**A 를 권한다.** GitHub OAuth 는 이미 붙어 있고, 우리 서비스는 어차피 GitHub 활동을
모아야 하므로 GitHub 계정 연결이 **필수**다. 사원 선택은 그 위에 한 번만 얹으면 된다.
다만 C 를 먼저 확인해 보는 편이 좋다 — 있으면 A 보다 낫다.

**그 밖에 걸리는 것 둘**

1. **회사가 둘인데 이름이 같다** — `coSeq` 값이 다른 두 회사의 `coNm` 이 같다.
   어느 쪽 사원 목록을 쓸지 정해야 한다.
2. **프론트에서 직접 못 부른다** — 사내망 주소이고 헤더에 키가 들어간다. 브라우저에서
   부르면 키가 노출되고 사외에서는 접근도 안 된다. **백엔드가 대신 부르고 우리 API 로
   중계**해야 한다. 담당자 2 의 작업이 하나 는다.
3. **우리 `users` 와 잇는 방법** — `empSeq` 를 `users` 에 저장해 두고 대조한다.
   컬럼 추가가 필요하다 (role 과 같은 마이그레이션에 묶으면 된다).

이 결정은 백로그 §2(자체 회원 체계)와 F-4·F-5 를 **대체한다.** 회원을 우리가 새로 만드는 게
아니라 **이미 있는 회원 조회 API 를 쓰는 쪽**으로 정해졌으므로, V4 마이그레이션과 자체
회원가입 폼은 범위에서 빠진다. 다만 `users` 에 **role 컬럼은 여전히 필요하다** — 관리자
콘솔에 들어갈 수 있는 사람을 가려야 한다 (§1-3).

### 1-2. 메뉴 재구성 (담당자 1) — 오늘의 주 작업

지금 메뉴는 **홈 · 초안 · 리포 · 인원 · 설정** 다섯이다. 회의에서 정한 것은 넷이다.

| 새 메뉴 | 지금 | 할 일 |
|---|---|---|
| **깃허브 내역** | 홈(`/`)의 타임라인이 겸하고 있다 | 전용 화면으로 분리. **행마다 드롭다운으로 설명**을 펼치고, 클릭하면 **상세 페이지**로 이동 |
| **VS 내역** | **없다.** 홈의 "미커밋 세션" 카드 숫자뿐 | 신규 화면. `GET /vscode/sessions` 는 이미 있다 — 세션별 미커밋·TODO·계획·저장 이벤트를 편다 |
| **업무 일지 작성** | `/drafts/:id` 는 있으나 목록 화면이 없다 | ✅ 만들었다. 날짜별 목록(2주/한 달/3개월) + 선택한 날짜의 생성·재생성. 탭 이름은 "초안 작성" 이 아니라 **업무 일지 작성** |
| **설정** | API Key, Mattermost, LLM(목업) | 아래 셋으로 재편 |

**설정 안에 들어갈 것**

- **API 연동** — VS Code 용 API Key 발급·재발급 (이미 있다)
- **깃허브 연동** — 내 계정에 GitHub 붙이기 (§1-1 과 같은 것)
- **리포지터리** — **전체 등록**과 **전체 동기화**. 지금은 한 건씩만 된다

> **확인 필요**: "전체 등록" 이 조직 전체 리포를 한 번에 등록한다는 뜻이면 **서버에 없는
> 기능**이다 (지금은 `POST /repos` 로 한 건씩). 담당자 2 와 맞춰야 한다. §5-2.

**계약 변경 하나** — `GET /drafts` 에 `from`·`to` 를 더했다. 날짜 하나로만 조회되던 것을
기간으로도 볼 수 있게 한 것이다. 기존 `date` 호출은 그대로 동작한다. 초안 CRUD 는 담당자 1
소유라(PRD §2) 직접 고쳤고, PRD §7 표도 함께 고쳤다.

**사라지는 것**: `인원` 메뉴는 관리자 콘솔의 "팀원 전체 내역"으로 흡수된다.
`홈`은 깃허브 내역으로 대체된다 — 다만 요약 카드 4개를 어디에 둘지는 정해지지 않았다 (§5-3).

**상세 페이지에 관하여**: `GET /activities/{id}` 는 이미 있고 `rawDiff` 필드도 선언돼 있다.
다만 **수집기가 채우지 않아 항상 null 이다** (백로그 F-2). 상세 페이지는 지금 만들 수 있지만,
코드 변경 내용은 비어 있다. 담당자 2 의 diff 수집이 붙어야 채워진다.

### 1-3. 관리자 콘솔 (담당자 2)

| 회의 내용 | 지금 상태 |
|---|---|
| 팀원 전체 내역 | `GET /stats/people` 미구현 (2-14). 프론트 `/people` 화면은 완성돼 목업 폴백 중 |
| LLM 모델 선택 | `GET/PUT /settings/llm` 미구현 (2-14). 프론트 화면 완성, 목업 폴백 중 |
| Mattermost 웹훅 연결 | 코드·실패 경로는 됨. **webhook URL 만 있으면 끝** |
| 회사 직원 목록 + **GitHub 활성화 상태** | **없다.** 미가입 기여자는 `external_login` 으로만 남는다 (백로그 §3-4) |

**담당자 1 이 넘길 것**: `/people`·`/settings` 화면은 이미 만들어져 있으니 관리자 콘솔에서
재사용할 수 있다. 필요하면 컴포넌트를 그대로 가져가면 된다.

**관리자 판별이 필요하다** — `users` 에 role 컬럼이 없다 (V1 확인). 콘솔 접근을 막으려면
마이그레이션이 하나 필요하다.

---

## 2. 오늘 한 일 (담당자 1, 완료)

VS Code 연동을 **실제로 설치해 쓸 수 있는 상태**까지 끌어올렸다. 커밋 8개, 전부 푸시 전이다.

| 커밋 | 내용 |
|---|---|
| `7c8ae4e` | **로그아웃 UI** — 상단 바 아바타 메뉴 (백로그 §3-3 해소) |
| `80f8e71` | **확장 패키징** `.vsix` + 설치 README. 설치 직후 API Key 안내 (F-6) |
| `b9b5b26` | 수집기 미리보기 하네스 + **새 파일 줄 수 오차 수정** |
| `90418dd` | 세션 확인 스크립트 `scripts/check-session.sh` |
| `66767a3` | **사이드바 뷰** — 지금 보낼 내용 (PRD X3 / 1-14, "여유 시" 항목이었다) |
| `a79ef18` | **계획 메모 여러 건** + 저장 기록·계획 재시작 후 유지 |
| `06c1b67` | **미푸시 커밋 표시** |
| `528963c` | **초안 근거를 GitHub/VS 두 갈래·세부 카테고리로** |

**실증된 것**: 설치본으로 API Key → 세션 전송 → DB 반영 → UPSERT(2회 전송에 행 불변)까지
확인했다. 세션 id 5, 리포 매핑 `uxis-co-kr/2026-pknu-3day`, 계획 메모 반영 확인.

**남은 것**: E2E 시나리오 5 의 **화면 확인**(홈의 미커밋 세션 카드, 재생성 후 "진행 중 / 미커밋").
메뉴가 재구성되면 이 확인도 새 화면 기준으로 다시 해야 한다.

---

## 3. 오늘 발견한 문제

### 3-1. 18:00 자동 생성 — 표시로 해결했다 ✅

`DraftScheduler` 가 매일 18:00 KST 에 **전 사용자 초안을 자동 생성한다** (`DRAFT_SCHEDULE_CRON`,
현재 설정돼 있음). "전송하면 초안으로 바로 만들지 말고 생성/재생성을 눌렀을 때 모아서 만들자"는
방향과 어긋나는 유일한 지점이다.

VS 전송 자체는 초안을 만들지 않는다 — `VscodeSessionService.upsert` 는 세션만 저장하고,
초안은 `POST /drafts/generate` 를 부를 때만 만들어진다. **자동 생성만 예외다.**

**끄지 않고 표시하기로 했다** (9/10 결정). V4 로 `drafts.auto_generated` 를 더해,
스케줄러가 만든 초안에는 화면이 **"자동 생성됨"** 을 붙인다. 내가 만들지 않은 초안이 왜
있는지 알 수 있으면 충돌이 아니다. 재생성하면 사용자가 만든 새 버전이 되어 표시가 사라진다.

### 3-2. 미푸시 커밋을 보낼 자리가 계약에 없다 🔴

커밋했지만 **아직 푸시하지 않은 구간은 GitHub 수집기가 볼 수 없다.** 원격에 없으니 API 에
나오지 않는다. 개발자 입장에서는 분명히 한 일인데 대시보드에도 초안에도 없다.

확장은 오늘부터 이걸 **사이드바와 상태바에 표시한다** (`06c1b67`). 그러나 서버로는 보내지
않는다 — `POST /vscode/sessions` 요청 본문(PRD §7)에 담을 자리가 없다.

초안의 "진행 중 / 미커밋" 에 들어가야 그날 한 일이 온전해진다. **커밋 메시지는 이미 사람이 쓴
요약이라 미커밋 파일 목록보다 초안에 더 어울린다.**

넣으려면: PRD §7 스키마 → `SessionRequest` → `vscode_sessions` 컬럼 → `DraftTemplate`.
담당자 2 와 맞춰야 한다. §5-4.

### 3-3. 초안 템플릿이 계획 여러 건을 한 줄로 붙인다 🟡 (담당자 2)

확장이 계획 메모를 여러 건 받게 바뀌었고(`a79ef18`), 서버 계약을 지키려고 **줄바꿈으로 이어**
한 문자열로 보낸다. `DraftTemplate.plans()` 가 `planNote` 를 한 줄로 취급해 `- ` 하나만 붙이므로,
초안의 `## 계획 / TODO` 에 여러 계획이 **한 덩어리 불릿**으로 들어간다.

읽는 데 문제는 없지만 줄마다 불릿이 되는 편이 맞다. `planNote.split("\n")` 한 줄이면 된다.

### 3-4. 수집기가 기본 브랜치만 본다 🔴

**오늘 내 커밋이 대시보드에 하나도 없다.** 원인을 찾아보니 수집 범위 문제다.

`GitHubApiClient` 가 `GET /repos/{owner}/{name}/commits` 를 `sha` 파라미터 없이 부른다.
GitHub 은 그때 **기본 브랜치의 커밋만** 돌려준다. 작업 브랜치에만 있는 커밋은 머지되기
전까지 수집되지 않는다.

```
Ae-Ti      마지막 활동 9/9 17:14   ← 오늘 커밋 10건이 작업 브랜치에 있다
UngsikJo   오늘 커밋 2건            ← 기본 브랜치로 갔다
```

**모두가 작업 브랜치에서 일하는 팀이라면 업무 일지가 거의 비게 된다.** 머지하는 날에만
하루치가 몰려 잡힌다. "그날 한 일"을 모으는 도구로서 근본적인 구멍이다.

고치는 방법 셋. 어느 쪽이든 담당자 2 몫이다.

| 방법 | 내용 | 판단 |
|---|---|---|
| **A. 모든 브랜치** | `GET /repos/{o}/{r}/branches` 로 브랜치를 훑고 각각 커밋을 모은다 | 정확하다. API 호출이 브랜치 수만큼 는다 |
| B. 이벤트 API | `GET /repos/{o}/{r}/events` 의 PushEvent 를 쓴다 | 한 번에 모든 브랜치. **최근 90일·300건 제한**이 있다 |
| C. 웹훅 | push 웹훅을 받는다 | 가장 정확하고 실시간. 사외 접근이 필요해 사내망에서는 어렵다 |

**A 를 권한다.** 리포 수가 적고, 이미 10분마다 도는 스케줄러가 있다. 중복은 지금도
`(repo, type, external_id)` 유니크로 걸러진다.

**임시 처치**: 깃허브 내역이 비었을 때 "기본 브랜치에 머지되기 전이라 그럴 수 있다"는
안내를 화면에 넣었다. 빈 화면을 수집 고장으로 오해하지 않게.

### 3-5. 오늘 수집된 커밋이 전부 미가입 기여자 것이다 🟡

오늘 수집된 커밋 2건은 모두 `UngsikJo` 인데 `user_id` 가 NULL 이다. 화면은 사용자에
연결되지 않은 활동을 거르므로 **아무것도 안 보인다.**

백로그 §3-4 와 같은 뿌리다. 사원번호 로그인이 붙고 관리자 콘솔에서 GitHub 계정을 이어 주면
풀린다 (§1-1, §1-3). 그전까지는 담당자 2 가 한 번 로그인하면 소급 연결된다.

### 3-6. 오늘 고친 것 (기록용)

| 문제 | 원인 | 커밋 |
|---|---|---|
| **저장 이벤트가 재시작하면 사라졌다** | 메모리에만 있었고, 서버는 `editTimeline` 을 통째로 덮어쓴다. 실제로 09:44 에 1건이던 것이 09:54 에 0이 됐다 | `a79ef18` |
| 새 파일 `+N` 이 git 보다 1 컸다 | 파일이 개행으로 끝나면 `split('\n')` 의 마지막 빈 조각까지 줄로 셌다 | `b9b5b26` |
| 설치 직후 무엇을 할지 알 수 없었다 | API Key 미설정 시 상태바 경고만 뜨고 눌러도 재전송만 반복 | `80f8e71` |

> 계획 메모는 담당자 2 가 서버에서 이미 지켜 두었다(`planNote` 가 null 이면 기존 값 유지).
> **같은 논리가 `editTimeline` 에는 적용되지 않았다.** 미커밋 파일·TODO 는 git 에서 매번 새로
> 뽑으니 덮어쓰는 것이 맞다.

---

## 4. 백로그에서 넘어온 것

[BACKLOG.md](BACKLOG.md) 의 항목 중 **오늘 살아 있는 것만** 옮겼다. 회의 결정으로 대체된
것은 표시했다.

| 출처 | 항목 | 담당 | 상태 |
|---|---|---|---|
| F-1 | 초안을 에이전트가 통으로 요약 (고정 템플릿 폐기) | 담당자 2 | 🔴 다음 사이클. PRD F3 개정 선행 |
| F-2 | 코드 변경 이력 표시 — 소스트리처럼 | 양쪽 | 🔴 **§1-2 상세 페이지가 이걸 기다린다.** `raw_diff` 가 비어 있다 |
| F-3 | Mattermost 전송 | 담당자 2 | ⏸ webhook URL 만 있으면 끝. 관리자 콘솔로 들어간다 (§1-3) |
| F-4·F-5 | 회원 체계 · 아이디 발급 | — | ✅ **회의에서 대체됨** → §1-1 (회원 조회 API 사용) |
| F-6 | VS 연동 구현·사용자 테스트 | 담당자 1 | 🟢 거의 완료 (§2). 화면 확인만 남음 |
| F-7 | 디자인 검토 | — | ❓ 범위 미확정. **메뉴 재구성(§1-2)으로 화면이 바뀌므로 그 뒤에 보는 게 맞다** |
| §3-1 | 리포 삭제에 소유자 검사 없음 | 담당자 2 | 🔴 미수정. 데이터 유실 위험 |
| §3-2 | `notify` 가 확정 여부를 검사하지 않음 | 담당자 2 | 🔴 미수정. webhook 붙는 순간 발현 |
| §3-3 | 로그아웃 UI | 담당자 1 | ✅ 완료 (`7c8ae4e`) |
| §3-4 | 미가입 기여자가 화면에 안 뜸 | 담당자 2 | 🟡 **관리자 콘솔의 "GitHub 활성화 상태" 가 이것이다** (§1-3) |
| 1-11 | E2E 시나리오 점검 | 담당자 1 | 🟡 5번 화면 확인 남음. 메뉴 재구성 후 다시 |
| 1-12 | `.vsix` 패키징·설치 README | 담당자 1 | ✅ 완료 (`80f8e71`) |
| 1-14 | 확장 사이드바 뷰 | 담당자 1 | ✅ 완료 (`66767a3`). "여유 시" 항목이었다 |
| 2-14 | `GET /stats/people`, `GET/PUT /settings/llm` | 담당자 2 | 🔴 **관리자 콘솔이 이 둘을 쓴다** (§1-3) |

---

## 5. 결정이 필요한 것

| # | 질문 | 정해야 할 사람 |
|---|---|---|
| 1 | ~~18:00 자동 생성 유지 여부~~ → **유지하되 "자동 생성됨" 으로 표시한다** (9/10 결정). V4 `auto_generated` | ✅ |
| 2 | 설정의 **"리포지터리 전체 등록"** 이 조직 전체를 한 번에 등록한다는 뜻인지 (§1-2) | 기획 / 담당자 2 |
| 3 | ~~요약 카드 자리~~ → 갈라진 화면에 나눠 담았다. 다른 안이 있으면 알려 줄 것 (§1-2) | ✅ |
| 3-1 | **수집기가 기본 브랜치만 본다** (§3-4) — 작업 브랜치 커밋이 일지에서 빠진다. A(모든 브랜치)를 권한다 | 담당자 2 |
| 4 | **미푸시 커밋**을 서버로 보낼지 — 계약 변경이 필요하다 (§3-2) | 기획 / 담당자 2 |
| 5 | ~~본인 확인 방식~~ → **정해짐**: 사원번호 아이디·비밀번호 (§1-1) | ✅ |
| 5-0 | ~~최초 로그인 시 비밀번호 변경 강제~~ → **강제하지 않는다** (9/10 결정). 설정에서 안내만 한다 | ✅ |
| 5-0-1 | `users` 를 **언제 채울지** — 최초 로그인 시 자동 생성 / 관리자가 일괄 등록 | 기획 / 담당자 2 |
| 5-1 | ~~회사가 `coSeq` 둘인데 이름이 같다~~ | ✅ **29 확정** (9/10). 사원 목록이 완전히 다르다 — 1 은 16명, 29 는 33명이고 사번 구간도 겹치지 않는다 |
| 5-2 | 와플 API 를 **백엔드가 중계**할지 (사내망 + 키 노출) — 프론트 직접 호출은 불가 | 담당자 2 |
| 6 | 관리자 판별용 **role 컬럼**을 언제 넣을지 (§1-1, §1-3) | 담당자 2 |
| 7 | 확장이 **미등록 리포**의 작업도 보낼지 (백로그에서 이월) | 기획 |

> **7번 부연**: 워크스페이스가 git 저장소이기만 하면 등록 여부와 무관하게 보고한다.
> 개인 프로젝트를 열어 두면 그 미커밋 작업이 회사 대시보드로 올라간다. 확장 README 에
> 주의로 적어 두었으나, 기획 판단은 아직이다.

---

## 6. 현재 시스템 상태

```
backend  :8080   Spring Boot · LLM 프로바이더 gemma4
frontend :5173   VITE_USE_MOCK=false. /settings/llm, /stats/people 만 목업 폴백
postgres :5433   Flyway V3 까지 적용
확장     설치됨  withly.worklog-drafter 0.1.0 (.vsix 로 설치, 사이드바·미푸시 포함)
```

- 등록 리포 2개 — `uxis-co-kr/2026-pknu-3day`, `Ae-Ti/CodeAtlas`
- 회원 1명 (`Ae-Ti`). `UngsikJo` 는 미가입이라 커밋이 미연결
- VS 세션 1건 (오늘, 미커밋 1파일, 계획 1건)
- 프론트 빌드·린트 통과, 확장 컴파일 통과

### 확인용 명령

```bash
scripts/check-session.sh --files              # 확장이 보낸 세션을 DB 에서 확인
cd vscode-extension && npm run preview -- .   # 전송 없이 수집 결과만 미리보기
```

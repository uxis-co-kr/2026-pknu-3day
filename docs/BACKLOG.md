# 미결 사항 정리 — 기획 변경 · 발견한 문제 · 남은 작업

작성: 2026-09-09 17:05 · 작성자: 담당자 1 (클라이언트 트랙)
기준: `main` = PR #4 머지 이후 · 담당자 1 브랜치 `VsPeristalsis_dashboard` = `59a3a59`

> 2일차까지의 구현은 끝났다. 이 문서는 **아직 결정되지 않았거나 손대지 않은 것**만 모은다.
> 완료된 내용은 [DAY1_client_result.md](DAY1_client_result.md), 일정 체크는 [TODO_client.md](TODO_client.md).

---

## 0. 한눈에

| 구분 | 항목 | 상태 |
|---|---|---|
| **기획 변경** | 자체 회원 체계 도입 | 🔴 결정됨, 미착수 (일정 밖) |
| **발견한 문제** | 리포 삭제에 소유자 검사 없음 | 🔴 미수정 (담당자 2) |
| | `POST /drafts/{id}/notify` 확정 검사 없음 | 🔴 미수정 (담당자 2) |
| | 로그아웃 UI 부재 | 🔴 미착수 (담당자 1) |
| | 미가입 기여자가 화면에 안 뜸 | 🟡 안내 문구만, 근본 해결은 회원 체계 |
| **남은 계획 작업** | 1-11 E2E 점검 / 1-12 패키징 | ⬜ 3일차 |
| | 2-14 `/stats/people`, `/settings/llm` | ⬜ 3일차 (담당자 2) |

---

## 1. 기획 변경 — 자체 회원 체계

### 배경

3일차 논의에서 나온 요구사항 셋 중 두 개가 **현재 인증 구조로는 만족되지 않는다.**

1. 등록한 리포의 **기여자 정보가 모두** 떠야 한다
2. 조회는 팀 전체 공개, **등록·전송은 본인 것만**
3. 한 기기에서 **여러 사람이 로그인·로그아웃** 할 수 있어야 한다

### 지금 구조와 그 한계

`users` 행을 만드는 유일한 경로가 GitHub OAuth 콜백이다. 즉 **GitHub 로그인이 곧 회원가입**이다.

```
users (github_id UNIQUE, login UNIQUE, name, avatar_url, github_token_enc)
  ↑ 이메일도 비밀번호도 부서 정보도 없다
```

로그인하지 않은 사람은 시스템에 존재하지 않으므로, 아래가 **구조적으로 불가능**하다.
실제 DB 확인 결과다.

```
UngsikJo (미가입) | 커밋 26 | VS 세션 0 | 초안 0
```

- `api_keys.user_id` NOT NULL → 회원이 아니면 API Key 를 못 받고, VS Code 확장을 연동할 수 없다
- `vscode_sessions.user_id` NOT NULL → 세션 자체가 존재할 수 없다
- `drafts.user_id` NOT NULL → 초안 생성 대상이 아니다

GitHub 커밋만 `external_login` 으로 남는다. **그 사람의 VS 작업과 업무 일지는 아예 없다.**

### 결정

**자체 회원가입을 도입한다.** 이유는 둘이다.

1. **팀원 목록을 미리 관리한다** — 관리자가 회원을 등록해 두고, 각자 자기 계정에 GitHub·VS Code 를 연결한다. 지금은 본인이 GitHub 로그인을 하기 전까지 시스템에 아무 자리도 없다.
2. **사내 정보를 받는다** — 이름·이메일·부서 같은 값은 GitHub 프로필에 없다.

즉 회원이 먼저 있고, **GitHub 과 VS Code 는 그 회원에 붙는 연동 수단**이 된다. 지금은 GitHub 이 곧 정체성이라 순서가 반대다.

### 바뀌어야 하는 것

| 영역 | 내용 |
|---|---|
| 스키마 | `users` 에 email · password_hash · 부서 등 추가, `github_id`/`login` 을 NULL 허용으로. **V4 마이그레이션** |
| 인증 | 자체 로그인으로 JWT 발급. GitHub OAuth 콜백의 의미가 "로그인" → **"내 계정에 GitHub 연동"** 으로 바뀐다 |
| 화면 | 회원가입 · 로그인 폼 · 연동 관리 화면 신규. 디자인 브리프 3.1 은 "GitHub로 로그인" 버튼 **하나**뿐이라 재설계가 필요하다 |
| 문서 | PRD §5 F5(인증), §6 데이터 모델, §12 확정된 가정을 다시 쓴다 |
| 코드 | 담당자 2 의 `auth/` 전반, 담당자 1 의 `LoginPage`·`AuthDonePage`·`RequireAuth` |

### 비용과 판단

**1~2일 규모다.** 3일차 하루에 착수하면 지금 동작하는 인증까지 흔들린다.
그래서 **이번 프로젝트 범위 밖으로 두고, 다음 작업으로 넘긴다.**

착수할 때 순서 제안:

1. PRD §5 F5 · §6 · §12 를 먼저 고친다 (계약 변경은 문서 먼저 — 프로젝트 규칙)
2. 디자인 브리프에 회원가입 · 로그인 · 연동 관리 화면을 추가하고 Figma 로 확정
3. V4 마이그레이션 → 자체 인증 → GitHub 연동 전환 → 화면
4. 기존 GitHub 전용 계정의 이관 경로를 정한다 (기존 `users` 행에 이메일을 나중에 채우는 방식 등)

### 이번 프로젝트에서 대신 할 수 있는 것

회원 체계 없이 요구사항 1 을 **부분적으로** 만족시키는 방법이다. 커밋은 보이지만 그 사람의
VS 작업·초안은 여전히 없다.

- 미가입 기여자를 `external_login` 이름으로 사용자 카드에 표시 (프론트 + `/stats/daily` 의 `byUser` 에 미가입 계정 포함)
- 현재는 **안내 문구 한 줄**만 있다 — "사용자에 연결되지 않은 활동 N건은 타임라인에 표시되지 않습니다"

---

## 2. 오늘 발견한 문제

### 2-1. 리포 삭제에 소유자 검사가 없다 🔴 (담당자 2)

```java
// RepoService.delete — 소유자 확인이 없다
public void delete(Long repoId) {
    Repo repo = repoRepository.findById(repoId).orElseThrow(...);
    repoRepository.delete(repo);
}
```

**팀원 누구나 남이 등록한 리포를 지울 수 있다.** `activities.repo_id` 가 `ON DELETE CASCADE` 라
그 리포의 활동 기록이 통째로 사라진다. 화면의 휴지통 아이콘 한 번이면 된다.

요구사항 2("등록·전송은 본인 것만")에 직접 걸리고, 그 이전에 사고 위험이다.
`registered_by != principal.id()` 면 403 으로 막아야 한다.

**함께 정할 것**: 동기화(`POST /repos/{id}/sync`)도 소유자만 할지. 읽기성 갱신이라
팀 누구나 눌러도 무해할 수 있다.

### 2-2. `POST /drafts/{id}/notify` 가 확정 여부를 검사하지 않는다 🔴 (담당자 2)

```java
// DraftNotifyController — 초안 존재만 확인하고 바로 전송한다
Draft draft = draftRepository.findById(id).orElseThrow(...);
if (!notifyService.notifyDraftContent(draft)) { ... }
```

DRAFT 상태 초안으로 호출해도 전송 단계까지 간다 (지금은 webhook 미설정이라 503 이 날 뿐).

디자인 브리프 3.3 은 "Mattermost 전송은 **확정 후에만** 활성", PRD F3 도 확정본을 보내는 흐름이다.
화면은 버튼을 잠가 막지만, API 직접 호출이나 `/external/*` 연동에서는 미확정본이 나갈 수 있다.
**webhook URL 이 붙는 순간 실제로 발생 가능하다.**

`status != CONFIRMED` 면 `409 DRAFT_NOT_CONFIRMED` 로 막아야 한다.
담당자 1 의 목업은 이미 그 규칙으로 동작하므로, 서버만 맞추면 양쪽이 같아진다.

### 2-3. 로그아웃 UI 가 없다 🔴 (담당자 1)

`auth.clear()` 는 401 을 받았을 때만 호출된다. 화면에 로그아웃 수단이 없다.

- 토큰이 localStorage 에 12시간 남는다
- 다른 사람이 쓰려면 주소창에 `:8080/api/auth/github` 를 직접 쳐야 한다. 동작은 하지만 화면에서 할 방법이 없다
- **디자인 브리프 2. 의 상단 바가 "날짜 선택기 + 아바타" 뿐이라 기획 단계에서 빠졌다**

아바타 클릭 → 사용자 이름 + 로그아웃 드롭다운이면 충분하다. 브리프에 없는 요소지만
없으면 서비스가 성립하지 않는다. **반나절 안 걸린다.**

### 2-4. 미가입 기여자가 화면에 안 뜬다 🟡

`HomePage.tsx` 가 `a.user !== null` 로 거르고, 사용자 카드는 `/stats/daily` 의 `byUser` 를 따른다.
`byUser` 는 매핑된 사용자만 담으므로 UngsikJo 의 커밋 26건이 화면에서 사라진다.

담당자 2 가 `unmapped` 필드를 추가해 **총계 = byUser 합계 + unmapped** 가 성립하게 해 줬고,
담당자 1 은 그 값이 0 이 아닐 때 안내 문구 한 줄을 띄운다. **근본 해결은 §1 회원 체계다.**

### 2-5. 오늘 고친 것 (기록용)

| 문제 | 원인 | 커밋 |
|---|---|---|
| 재생성 후 흰 화면 | `POST /drafts/generate` 응답을 `GET /drafts/{id}` 캐시에 넣었다. 근거가 id 배열 vs 객체 배열로 모양이 다르다. 목업이 상세 모양을 돌려줘 가려져 있었다 | `be292b4` |
| 발급 응답 타입 오류 | `IssuedApiKey extends ApiKey` 인데 실서버 응답에 `lastUsedAt` 이 없다 | `59a3a59` |
| PR 제목 중복 표기 | 수집기가 `PR_MERGED` 의 title 에 접두사를 붙여 저장 | 담당자 2 가 V3 로 수정 |
| PR 행에 `+0 −0` | `files_changed` 등이 NOT NULL DEFAULT 0 이라 PR 도 0 으로 온다 | `f01ff06` |

---

## 3. 남은 계획 작업

### 담당자 1

| ID | 작업 | 비고 |
|---|---|---|
| 1-11 | E2E 시나리오 1·3·4·5·9 점검 | 1·3·4·9 는 확인됨. **5번(홈의 미커밋 세션 카드)은 화면 확인이 남았다** — 확장→서버→DB 까지만 봤다 |
| 1-12 | `vsce` 로 `.vsix` 패키징, 프론트 빌드, 설치 README | 확장은 `vscode` 모듈을 흉내 낸 하네스로만 검증했다. **실제 설치 후 동작은 여기서 처음 본다** |
| 1-14 | (여유 시) 모바일 열람 UI, 확장 사이드바 뷰 | P2 초과 |
| — | 로그아웃 UI (§2-3) | 계획에 없던 항목 |

### 담당자 2

연동 트랙의 **P0·P1(F1·F2·F3a·F5·F7·F8)은 전부 붙었다.** 커밋 19개, 단위 테스트 90개.
아래는 [DAY1_arrange.md](DAY1_arrange.md) 의 3일차 계획에 §2 의 신규 항목을 더한 것이다.

| ID | 작업 | 상태 · 비고 |
|---|---|---|
| 2-12 | E2E 시나리오 2·6·7·8 점검 | **2·6·8 은 이미 검증됨** (sync 2회 건수 불변 / `/external/summary` API Key 200·JWT 403 / 실제 LLM 요약). **7번(Mattermost)만 webhook 대기** |
| 2-13 | 미커밋 리마인드 스케줄러 (F7-2) | 확장이 세션을 보내므로 실데이터로 검증 가능해졌다 |
| 2-14 | `GET /stats/people`, `GET/PUT /settings/llm` | **담당자 1 의 `/people`·`/settings` 화면이 이것만 기다린다.** UI 는 1일차에 완성돼 목업 폴백 중이라, 엔드포인트만 오면 폴백 목록에서 지우면 끝난다 |
| — | Mattermost 실전송 검증 | 코드·설정·실패 경로는 확인됨. webhook URL 만 받으면 F7 종료 |
| — | **리포 삭제 소유자 검사** (§2-1) | 계획에 없던 항목. 데이터 유실 위험이라 우선순위 높다 |
| — | **notify 확정 검사** (§2-2) | 계획에 없던 항목. webhook 붙는 순간 발현된다 |

**우선순위 제안**: 2-1 · 2-2 (사고 위험) → 2-14 (담당자 1 대기 해제) → Mattermost 실전송 → 2-13 → 2-12.
담당자 2 가 "2-14 를 2-13 보다 앞으로 당길 수 있다"고 물어왔고, 담당자 1 쪽 화면이 이미 완성돼 있으므로
**당기는 쪽이 맞다.**

### 담당자 2 가 물어온 확인 사항 — 담당자 1 답변

[DAY1_arrange.md](DAY1_arrange.md) §6 에 다섯 가지가 있다. 화면 쪽 판단이라 여기 답을 적어 둔다.

| # | 질문 | 답 |
|---|---|---|
| 1 | `todayActivityCount` 는 커밋만인가 | **커밋만이 맞다.** 아트보드 5 의 예시 값(punchcheck 9 · salty-web 3)이 리포별 커밋 수와 정확히 일치한다. 이름이 오해를 부르는 건 동의하지만, 화면 라벨이 "오늘 활동 수"라 필드명은 그대로 두고 PRD 에 정의만 명시하는 편이 낫다 |
| 2 | 미가입 author 활동의 화면 표기 | 지금은 **타임라인에서 제외 + 안내 문구 한 줄**이다. 아바타·이름 자리를 만들려면 §1 의 회원 체계가 전제다. 그 전까지는 현행 유지 |
| 3 | `syncStatus = FAILED` 해제 시점, 재시도 버튼 | **별도 재시도 버튼은 두지 않는다.** 리포 관리 화면에 이미 동기화 아이콘 버튼이 있고, 그걸 누르면 재시도된다. 버튼을 하나 더 두면 디자인에 없는 요소가 늘어난다 |
| 4 | `staleSessions` 6시간 기준 | F7-2 리마인드와 같은 값으로 통일한 것에 동의한다. 화면 문구도 "6시간 이상"으로 고정돼 있다 |
| 5 | 초안 목록에서 최신 버전 고르기 | **이미 그렇게 구현했다.** `GET /drafts` 가 `findLatestByWorkDate` 로 `(user, date)` 당 최신 버전만 준다. 홈은 사용자당 배지 하나를 그린다 |

### 공통

- **동기화 포인트 ③** — 최종 머지, 전체 E2E 재실행, 태그 `v0.1.0`

---

## 4. 결정이 필요한 것

| # | 질문 | 정해야 할 사람 |
|---|---|---|
| 1 | 회원 체계(§1)를 다음 사이클에 넣을지, 넣는다면 언제 | 기획 |
| 2 | `POST /repos/{id}/sync` 도 소유자만 할지 | 기획 / 담당자 2 |
| 3 | 확장이 **미등록 리포**의 미커밋 작업도 보낼지 | 기획 / 담당자 1 |
| 4 | 2-14 를 2-13 보다 앞으로 당길지 (담당자 1 화면이 대기 중) | 담당자 2 |

> **3번 부연**: 지금 확장은 워크스페이스가 git 저장소이기만 하면 등록 여부와 무관하게 보고한다.
> 개발자가 개인 프로젝트를 VS Code 로 열어 두면 그 미커밋 작업이 회사 대시보드로 올라간다.
> 요구사항 2 의 문구("등록한 리포 **또는** VS 에서 전송한 내용")로는 허용되지만, 프라이버시 관점에서
> 짚어 둘 값어치가 있다.

---

## 5. 현재 시스템 상태

```
backend  :8080   Spring Boot · LLM 프로바이더 gemma4 (실제 요약 동작)
frontend :5173   VITE_USE_MOCK=false (실서버). /settings/llm, /stats/people 만 목업 폴백
postgres :5433   Flyway V3 까지 적용
```

- 백엔드 테스트 **114개 통과** (실패 0), 프론트 빌드 통과, 확장 컴파일 통과
- 등록 리포: `uxis-co-kr/2026-pknu-3day` — 커밋 27 · PR 4 · 머지 4 수집됨
- 회원: `Ae-Ti` 1명. `UngsikJo` 는 미가입 상태라 커밋 26건이 미연결
- 로컬 DB 이므로 **담당자 2 가 자기 로컬에서 로그인해도 이 DB 에는 반영되지 않는다**

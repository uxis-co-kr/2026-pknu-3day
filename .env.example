# docker compose 가 읽는 루트 환경 변수. 이 파일을 .env 로 복사해서 쓴다 (.env 는 커밋하지 않는다).
# 백엔드 애플리케이션용 변수는 backend/.env.example 에 따로 있다.

# postgres 컨테이너를 호스트에 노출할 포트.
# 로컬에 PostgreSQL 이 5432 를 점유한 개발기가 많아 기본값은 5433 이다.
# 바꾸려면 backend/.env 의 DB_URL 포트도 같은 값으로 맞춘다.
POSTGRES_PORT=5433
